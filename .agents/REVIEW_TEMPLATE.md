# Blaster 任务审查模板

在每个任务实施与验证后使用。本审查必须独立于实现过程重新判断事实；没有足够证据时，结论应为 `REVISE` 或 `BLOCKED`，不得推断为通过。

```md
# REVIEW-TASK-Bxx-简短名称

审查日期：YYYY-MM-DD
对应任务卡：TASK-Bxx-xxx
审查结论：PASS | PASS WITH FOLLOW-UP | REVISE | BLOCKED

## 1. 范围与可追溯性

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| 交付与 Gate 0 唯一目标一致 | 通过 / 不通过 |  |
| 所有改动都在白名单内 | 通过 / 不通过 |  |
| 非目标没有被偷偷并入 | 通过 / 不通过 |  |
| 课程来源、Transcript 状态如实记录 | 通过 / 不通过 |  |

## 2. UE5.6 兼容性

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| Build.cs、include、UHT 与编译通过 | 通过 / 不通过 / 未验证 |  |
| Enhanced Input / 生命周期符合 UE5.6 | 通过 / 不通过 / 不相关 |  |
| UPROPERTY、GC 和对象生命周期安全 | 通过 / 不通过 / 未验证 |  |
| 动画、移动、UI 的 Editor 行为已实际确认 | 通过 / 不通过 / 不相关 |  |

## 3. 多人正确性

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| Server 是权威状态的唯一写入者 | 通过 / 不通过 |  |
| RPC 方向、Owner 与调用时机正确 | 通过 / 不通过 |  |
| Replicated 属性已注册，OnRep/Multicast 职责合理 | 通过 / 不通过 / 不相关 |  |
| 客户端输入无法直接伪造关键结果 | 通过 / 不通过 |  |
| UI 只在本地创建/更新，初始化时序安全 | 通过 / 不通过 |  |
| Pawn 销毁、晚加入、重复事件等边界已处理 | 通过 / 不通过 / 不相关 |  |

## 4. 验证证据

| 层级 | 结论 | 证据 |
| --- | --- | --- |
| Development Editor Build | 通过 / 不通过 / 未运行 |  |
| Listen Server + 1 Client 主路径 | 通过 / 不通过 / 未运行 |  |
| 失败路径 | 通过 / 不通过 / 未运行 |  |
| 白名单外回归 | 通过 / 不通过 / 未运行 |  |

## 5. 学习与作品集真实性

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| 学习者能解释完整数据流 | 通过 / 不通过 |  |
| 有 2-3 道可复习面试题 | 通过 / 不通过 |  |
| AI、Editor 与用户工作边界如实标注 | 通过 / 不通过 |  |
| 已知限制没有被包装为已完成 | 通过 / 不通过 |  |

## 6. 问题清单

| 优先级 | 问题 | 影响 | 必须修复 / 后续跟进 | 证据 |
| --- | --- | --- | --- | --- |
| P0-P3 |  |  |  |  |

## 7. 结论与唯一下一步

- 结论理由：
- `PASS WITH FOLLOW-UP` 的不阻塞项：
- 若为 `REVISE`：返回本任务的最小修订目标：
- 唯一下一任务：
```
