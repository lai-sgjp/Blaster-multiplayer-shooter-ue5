# Every complaint people actually have about Unreal MCP servers, and what this project does about it

A living matrix. Each row is a complaint sourced from a real bug report, forum thread, or issue
tracker (linked), not a hypothetical. Status is written honestly:

- **Solved** - fixed in this repo.
- **Solved (verified)** - fixed *and* mechanically exercised against a real editor by a script that
  re-checks it. Reserved deliberately, because an unexercised guarantee has already turned out to
  be false here once (see E2).
- **Solved (by design)** - the architecture makes the complaint impossible.
- **Solved (optional)** - solved once a documented opt-in is configured.
- **Partly** - reduced but not eliminated.
- **Open** - still true here, and on the backlog.

The goal this feeds: someone with zero coding skill points any AI model at their project, asks for
a feature, and gets a working, readable Blueprint. Every row below is something that currently
stops that from feeling like magic.

Last updated 2026-08-18.

## A. Getting connected at all

This is the single largest category. Most people who try an Unreal MCP server never get past it.

| # | Complaint | Source | Status | What this project does |
| --- | --- | --- | --- | --- |
| A1 | Server starts, then "instantly drops connections" from Claude, Cursor, VS Code, Roo Code. `Connection closed Error -32000`. No workaround found. | [Epic forums, UE 5.8 experimental MCP](https://forums.unrealengine.com/t/5-8-experimental-modelcontextprotocol-mcp-server-instantly-drops-connections/2729488) | **Solved (by design)** | This project does not use HTTP/SSE. The transport is stdio to the MCP client and one line-delimited JSON request per TCP connection to the editor. There is no long-lived stream to drop, and no SSE keepalive to starve. |
| A2 | "MCP server connects but does nothing useful" until you manually enable Toolset registries. | [Epic forums](https://forums.unrealengine.com/t/5-8-experimental-modelcontextprotocol-mcp-server-instantly-drops-connections/2729488) | **Solved** | Enabling the plugin is the only step. There is no second opt-in registry, no per-toolset enablement, and every capability the plugin implements is exposed as a tool (enforced by `npm run check:parity`). |
| A3 | `Connection refused` with no indication of which of the five possible causes it is. | [PearceMullins/ue5-mcp troubleshooting](https://github.com/PearceMullins/ue5-mcp/blob/main/troubleshooting.md), [remiphilippe/mcp-unreal](https://github.com/remiphilippe/mcp-unreal) | **Solved** | `ECONNREFUSED` returns an ordered checklist naming the exact log line to look for (`UnrealMCPBridge: listening on 127.0.0.1:<port>`), the env var to fix a port mismatch, and the modal-dialog case. It also states plainly that nothing was sent, so nothing changed. |
| A4 | `MCP Unreal: Unexpected token 'C', Connection...` — the client got non-JSON and reported a parse error with no diagnosis. | Community reports, multiple projects | **Solved** | A non-JSON reply is diagnosed as "something other than the bridge is on this port, or the plugin build is older than this server", and echoes the first 200 bytes received. |
| A5 | Requires the Python Editor Script Plugin plus Remote Execution enabled in project settings. | [runreal/unreal-mcp](https://github.com/runreal/unreal-mcp), [kvick-games/UnrealMCP](https://github.com/kvick-games/UnrealMCP) | **Solved (by design)** | No Python. The bridge is C++ against Kismet2/EdGraph/AssetRegistry, so there is no scripting plugin to enable and no `execute_python` path to crash in. |
| A6 | Zombie Node processes have to be killed by hand; both the editor and the client need a full restart to reconnect. | [runreal/unreal-mcp](https://github.com/runreal/unreal-mcp) | **Solved (verified)** | The MCP server holds no editor connection between calls, so it cannot go stale. Audited rather than asserted: `npm run verify:restart` holds ONE long-lived client, kills the editor underneath it, restarts it, and confirms the next call reconnects with nothing done to the client - and that a real tool call, not just a ping, works afterwards. While the editor is down the failure is the diagnostic checklist rather than a raw socket code. |
| G13 | An agent reports a task finished and the work is not on disk. Only Blueprints and levels could be saved through the bridge, so every struct, enum, material, material instance and Data Table created through this server stayed dirty in memory until a human noticed and pressed Ctrl+S - and an editor crash erased it. | Found by auditing this project's own save path while adding Data Tables, not reported by anyone | **Solved** | The save helper never needed a Blueprint, only a UObject and its package. It now takes any asset, and `save_asset` exposes that, with the same source-control checkout handling every other save has. |
| G18 | "Cheap" and "works with weak models" are assumed to be a trade-off against capability, so tools ship a big surface and let the model sort it out. | Measured here across profiles, back to back | **Solved** | The opposite is true in measurement: `minimal` (10 tools, ~3.6k tokens) completes every benchmark task in **one call with zero failed calls**, while `lazy` (25 tools, ~9.2k) takes up to sixteen calls and seven failures for the same result. A smaller surface is cheaper AND more reliable, because there are fewer wrong paths to try first. |
| G19 | The real fear with an agent in an existing project is not that it fails, but that it quietly removes something that already worked - a Blueprint that loses a handler on Tuesday is found on Friday. | The most common concern raised about letting an agent touch a live project | **Solved** | Edits are additive and there is now a permanent test saying so: `live-verify` asserts a second `build_graph` adds to a graph rather than replacing it, and that the original handler still prints what it printed before - checked through the pin value, since a node can survive with its inputs reset and still look intact. Benchmarked end to end: a 7B adds a variable and a handler to an existing Blueprint in ~3 calls, 5/5, with everything existing intact. |
| G20 | UI cannot be automated: building a screen is a long sequence of small calls, and a weak model completes the first one and stops. Widget creation is one of the workflows people most want automated and least often get. | Measured here: a 7B failed a HUD task 3/3 through the step-by-step tools | **Solved** | `unreal_scaffold_widget` builds the Widget Blueprint and every element in it in one call, and is in the `minimal` profile - which previously had no widget tool at all, so the smallest profile could not build a UI. 0/3 to 3/3. |
| G21 | Creating a Blueprint from a UMG parent class silently produces a trap: the engine accepts it, every call reports success, and the asset cannot open in the UMG designer or hold widgets. | Found by watching a 7B fall back to it after another failure | **Solved** | `use_create_widget_blueprint` refuses it and names the command that does work. Nothing is created. |
| G22 | A server that enables tools at runtime is useless to a client that does not re-read the tool list, and the failure looks like the model inventing tool names. | Found in this project's own benchmark harness, which ignored `notifications/tools/list_changed` | **Solved** | The harness re-lists immediately after a tool-enabling call. Worth knowing for any client: the text-recovery path filters against the tool list too, so a stale list hides a newly enabled tool twice over. |
| G23 | Reading one real Blueprint costs more context than a small model has. A 104-node graph from a real project serialises to ~8,800 tokens - more than the entire `lazy` tool payload - so "can it work on a big Blueprint" was no. | Measured against a copy of a real 8-month-old project | **Solved** | Two thirds of that payload was 32-character node GUIDs and repeated JSON keys, not information. `unreal_explain_graph` returns what the graph does rather than how it is wired: **8,838 tokens to 915, and 357 for the text alone - 24x**. See [REAL_PROJECT_SCALE.md](REAL_PROJECT_SCALE.md). |
| G24 | "I describe a bug in plain words and it has to find the right Blueprint among hundreds." | The single most common description of what people actually want | **Solved** | Measured end to end on a real project: project shape 176 tokens, finding the system from one vague word 316ms, understanding what it does 915 tokens, scoring it ~100. **~2,700 tokens total**, and no graph is opened until the index has narrowed it to one. |
| G25 | A plugin that builds on two engine versions is assumed to work on both. | Found here: behavioural verification had only ever run on 5.6 | **Solved** | The suite now runs on 5.8 too, and the first run found node titles differing between versions (`Print String` vs `PrintString`) and a guard that could not fire on a project whose folders existed only in memory. Building is not exercising. |
| G26 | An agent turned loose on a big existing Blueprint will quietly break the systems it was not asked about. | The most common reason people will not let an agent near a real project | **Solved (verified)** | A `heavy` benchmark builds a twelve-system player Blueprint and asks for a thirteenth. Across every run the existing systems and variables survived intact - failures were always "did not finish", never "removed something". |
| G27 | A model gets stuck calling the same tool forever and burns the budget with nothing to show. | Measured here four times, in four unrelated tools | **Solved** | Not by explaining. `doctor`'s verdict was reworded (no effect), then a general repeat notice was added to every tool result and verified delivered (no effect on a 7B). Removing the argument-free tool from the smallest profile took the same tasks from 20 calls to 3-6. A weak model does not act on being told and does act on not being offered. |
| G28 | An agent produces Blueprints that compile and are still bad: work on Tick, level sweeps in hot paths, casts every frame. Compiling is the only signal anyone gets, and it is a very low bar. | The gap between "it works" and the AAA bar people actually mean | **Solved** | The quality gate now checks the architecture, not just the shape: `level-sweep-every-frame` and `spawn-every-frame` (errors), `cast-every-frame` (warning), `level-sweep-maybe-repeating` (info). Reachability from Tick is computed from the graph read that was already happening, so it costs nothing extra. Verified against a real project: it found the real cost and produced no false positives. |
| G29 | State ends up in the wrong place, which is invisible until a respawn and the most expensive thing in a Blueprint to retrofit. A score on a Character silently resets the first time somebody dies. | The judgment a small model cannot derive; raised as the thing that separates working from AAA | **Solved** | `state-outlives-owner` flags a score, name, team, currency or progression living on a Pawn or Character and names where it belongs - PlayerState, or the GameInstance if it survives level loads. `list_variables` now returns the parent class, because whether state is misplaced depends entirely on what holds it. Deliberately conservative: health is not flagged, and CamelCase is split so `ScoreboardWidget` is not mistaken for a score. |
| G30 | Multiplayer bugs survive every check there is: they compile, they review clean, they work with one player, and then the game is wrong for everyone but the host. | The class of bug people lose days to, and the reason many will not let an agent touch a networked project | **Solved (verified)** | `server-writes-unreplicated` walks execution from every `Server_` event and checks what it writes against the variable's replication flag, which `list_variables` now returns. Verified end to end against a live editor with a deliberately broken Blueprint, and silent on the real project, whose replication is correct. Silent on single-player Blueprints entirely. |
| G31 | A quality score that moves for reasons the report does not contain. | Found in this project's own review after adding Blueprint-level checks | **Solved** | Findings about the asset as a whole - where state lives, whether the server's writes reach clients - now come back in a `blueprint` array rather than only affecting the number. A score nobody can trace back to a reason is worse than no score. |
| G32 | Quality checks that only run when asked. An agent that does not know a review tool exists ships everything it writes unreviewed, which is most agents. | The gap between having checks and them mattering | **Solved** | Findings now travel with the writes that cause them: `add_variable` returns the misplaced-state warning with the write (the parent class rides along in the response, so no extra round trip), and `scaffold_blueprint` returns the Blueprint-level findings with its result. Nothing is added when there is nothing to say. Effect on a weak model is explicitly unmeasured; measured as not making anything worse. |
| G33 | "One Blueprint is connected to five others - how do you explain that to an AI effectively?" | The single hardest thing about working on an existing project, and the most common reason agents break one | **Solved (verified)** | `map_system` run against a real 8-month project with one word, no Blueprint named: 868ms, and it returned the component, both interfaces, the animation blueprint and the base character, plus a reading order and a blast-radius warning. |
| G34 | A map of a system costs more to read than the system. | Measured: the first real run was 3,396 tokens, one asset carrying 24 near-identical reasons | **Solved** | Reasons collapse to a sentence per asset and the map returns prose by default - 4,370 tokens to 523, **8.4x**. `detail: true` still returns exact paths and the reference graph. Second time this trade has paid off, after `explain_graph`. |
| G35 | A plugin built for one engine version and tested against another fails in a way that looks exactly like broken code, sending the investigation into the wrong codebase. | Cost time three times in this project alone | **Solved** | `ping` reports `pluginBuiltAt`, stamped at compile time, and `live-verify` refuses to start when the running plugin is older than the newest source. `npm run build:engines` builds every configured engine in one command and reports success only if every target actually built. A check that can be skipped by a person having a bad day is not a check. |
| G36 | A prebuilt plugin download that is older than the server it talks to. Every check passes, the protocol number matches, and then the first tool that needs a newer command fails with `unknown_cmd` and nothing explains why. | Found by cloning this repo fresh and following its own README as a stranger | **Solved** | `--doctor` probes for commands the plugin ought to have, calling each with no arguments so the probe is free, and names exactly which features are missing. The prebuilt links are gone from the README, with the reason stated. |
| G37 | Documentation that links to the author's own hard drive. | The root README linked to `file:///f:/Projects/...`, in the file a newcomer reads first | **Solved** | `check:docs` fails on any machine-local link target in any documentation file. Verified by adding one and watching it fail. |
| G38 | An MCP server tested only through one lenient client, which then misbehaves in every other one - the failure being invisible from the vantage point it was built at. | The general form of four separate harness bugs in this project | **Solved** | `npm run check:protocol` behaves like a client that forgives nothing: handshake and capability declarations, every tool name and schema, error shape for wrongly-typed and missing arguments, the prompts round trip, and concurrent requests. It runs without an editor and is part of `npm test`. It found nothing, which is stated plainly, and was verified against a deliberately broken server so that "found nothing" means something. |
| G39 | An automated cleanup that quietly changes behaviour, run by the person least able to notice. | Found by pointing this project's only editing tool at a real project for the first time | **Solved (verified)** | It reported it would remove 2 dead nodes while also saying 2 empty events were "only you know which was intended" - the same two nodes. An empty override event is not a stray expression: on a Blueprint whose parent is a Blueprint it suppresses the parent implementation, so deleting it changes behaviour. Events are excluded from dead-node removal, and the live suite now fails if cleanup ever deletes one. Verified it still removes genuinely stray nodes. |
| G40 | Referring to another Blueprint as a variable type fails with `class_not_found`, because a Blueprint's class path ends in `_C` and its asset path does not - which nothing tells you. | Found while building probes to test something else; it blocks the most ordinary thing there is, one Blueprint holding a reference to another | **Solved** | The class resolver accepts the asset path and resolves it to the class, the same way pin names accept a near miss. If the asset genuinely does not exist, the error explains the `_C` distinction rather than restating the name. |
| G41 | An agent deletes something the rest of the project depends on. | The unrecoverable failure, as opposed to the merely embarrassing ones | **Solved (verified)** | Verified against real cross-references: deleting a Blueprint with five referencers is refused and each blocker is named by path. The set logic is right too - deleting two assets that only reference each other succeeds, so retiring a whole system does not require `force`, which is what teaches people to pass `force` always. |
| G42 | "My game has bugs and a deadline. Where do I look?" - a question no per-Blueprint tool answers, and the one people actually arrive with. | The shape of every real request; a hands-on review noted a new user who can only say "it still doesn't work" ends up scrapping the project | **Solved (verified)** | `npm run audit` walks every Blueprint and ranks findings by likely cost rather than severity. Measured on a real 339-Blueprint game: 1,195 graphs and 16,187 nodes in **24 seconds**, 818 findings, and the top-ranked Blueprints were the player, GameState and GameMode. See [PROJECT_AUDIT.md](PROJECT_AUDIT.md). |
| G43 | A cast that fails does not error - the chain just stops, so the feature silently does not happen and there is nothing to search for. | 119 occurrences in one real project; the single most common silent bug in Blueprints | **Solved (verified)** | Reported as the highest-cost finding class, with the graph and the target class named. On the real project this surfaced a client-side cast to the GameMode in the death sequence - GameMode exists only on the server, so on every non-host machine the spectator, vacuum shutdown, timer clear and widget close all silently never ran. |
| G44 | Every MCP command against the editor is slow for no visible reason, which makes an agent feel sluggish however cheap its tokens are. | Measured here: wildly different commands all took ~333ms while `ping` took 8ms | **Solved** | The editor's "Use Less CPU when in Background" setting collapses its tick rate whenever it is not the foreground window - which is always, when an agent is driving it. The bridge disables it for the session, logs why, and writes nothing to the user's config (`-MCPKeepEditorThrottle` opts out). **One graph read went from 333ms to 20ms, and a 339-Blueprint audit from ~12 minutes to 74 seconds.** |
| G45 | Casting to a GameMode from anything that runs on a client fails silently on every client, and nothing warns you. | Found by hand in a real project's death sequence, then generalised | **Solved (verified)** | `cast-to-server-only-class`, using `describe_class` to ask the engine for real ancestry rather than guessing from names. A GameMode casting to a GameMode is allowed, and a cast behind `Switch Has Authority` is allowed - both detected, not assumed. Found 13 real cases in a 339-Blueprint project. |
| G46 | The agent cannot answer "my game has bugs, where do I look?" - every tool is about one asset, and whole-project analysis is something a human runs by hand, if it exists at all. | The question every real request starts from | **Solved** | `unreal_audit_project` audits every Blueprint and returns a ranked plan rather than a list: grouped by kind with examples, the Blueprints worth opening first by accumulated cost, and one next action. Measured on a real project: 250 findings from 60 Blueprints in 10s, in a **~2,570 token** reply. |
| G47 | The tool that answers the user's question exists and the model never calls it, reaching instead for whatever sounds vaguely right. | Measured here a fourth time, with `unreal_audit_project`: 0/3 | **Solved** | One sentence pointing at it, placed in the two tools the model actually called. 0/3 to 3/3, with the other tasks re-run to confirm the added text cost nothing. The rule, now four for four: the redirect must be where the model is already looking, and it must be one sentence. |
| G48 | A character pushed or dragged by another player stutters and rubber-bands for everyone except the host, and it gets blamed on the connection. | Reported from a real playtest and diagnosed from the Blueprints alone | **Solved (documented)** | The handbook now has the pattern: character movement is predicted on the owning client, so a force applied only on the server guarantees correction. The give-away is a Tick chain that branches on variables which are not replicated - it looks like it runs everywhere and does nothing on the client. Fixes are listed cheapest first. |
| G49 | "Sometimes a computer just can't host a lobby, and other times computers just can't find lobbies." The build works, then it does not, and nobody can reproduce it alone. | Reported from a real LAN playtest, then found in the Blueprints | **Solved** | `unreal_audit_project` now checks whether the way a project HOSTS a session matches the way it SEARCHES for one. A LAN session is invisible to an online query and the reverse; hosting succeeds, searching succeeds, the list is empty, and nothing reports an error. It ranks at cost 100 - the same tier as a cast that only fails on clients - because it cannot be seen on one machine. Found on a 339-Blueprint project in 65 seconds: four live host paths, three online and one LAN, against a single online search. |
| G50 | "My UI updates for the host but not for anyone else." The cast succeeds, nothing errors, and it works every time you test it alone. | Reported constantly in Unreal multiplayer help threads; found twice in one real project | **Solved** | `unreal_audit_project` reports `server-event-touches-widget`: a Server RPC that creates, removes or casts to a widget. A widget exists only on the machine that created it, so the work lands on the host's screen and nowhere else - and on a listen server the host IS the server, which is why it survives testing. Cast targets are judged widgets by class ancestry, never by name. Found a ping tag that only the host could see. Authority is followed backwards across interface messages, plain calls and timer bindings, so the report names the Server RPC that put the chain on the server even when it is four calls away in a different asset. **Limit:** a timer bound through `Create Event` is not followed - the bound function's name is not in the node. |
| G51 | A value is replicated and the clients still do not react to it. | Found in a real project, 27 times | **Solved** | `unreal_audit_project` reports `repnotify-does-nothing`: a variable marked RepNotify whose OnRep function is empty. Marking it RepNotify is a statement that the clients must react when it arrives, so an empty one is usually the missing half of a feature - and usually the half that works for everybody who is not hosting. |
| G52 | "I pressed play and a crap ton of errors showed up." The log scrolls faster than anyone can read, the same line repeats thousands of times, and an AI assistant driving the editor cannot see any of it. | Reported mid-session on a real project | **Solved** | `unreal_read_runtime_errors` reads the editor's own log and reports the last Play In Editor session, grouped and ranked. It is the only tool here that sees runtime problems - every other one reads the graph, and `Accessed None trying to read property X` does not exist until the game runs. Those lines name the exact Blueprint, graph and node, and come back as fields rather than as prose. On a real project: 2,074 error and warning lines, 15 distinct causes, the worst of them 54 times. It reads the log file rather than the editor, so it works while the editor is mid-crash and can read the session that already happened - which is always the situation when somebody asks. |
| G53 | A child Blueprint overrides an event and the parent's version silently stops running. Everything the parent set up is missing, on every machine, and nothing warns. | Found from a runtime error on a real project | **Solved** | `unreal_audit_project` reports `parent-event-not-called`. Adding `Event BeginPlay` to a child does not extend the parent's - it replaces it unless a `Parent: BeginPlay` node is placed by hand. The child's own logic works perfectly, so the Blueprint looks correct. The report always names what is being skipped, because that is the sentence someone needs to judge whether the override was deliberate. Found the cause of 54 runtime errors a session: a player Blueprint whose parent's BeginPlay created the component its whole vacuum feature depended on, and three player controllers that all skipped a base class BeginPlay which creates a widget. `unreal_build_graph` can now place the fix as well as find it: `nodeType: "CallParent"` builds the `Parent: BeginPlay` node, accepting the name as the editor shows it. |
| G14 | Passing a folder where an asset path was meant silently creates an asset named after the folder, so the thing you asked for never exists and every later call against it fails with a confusing "not found". | Found by re-running the local-model benchmark; a 7B did exactly this, but nothing stops a person doing it | **Solved** | `path_is_a_folder` refuses it and names the fix. A folder on disk at that exact path is unambiguous evidence of the mistake. The check is in the shared path validator, so Blueprints, structs, enums, materials, levels and Data Tables are all covered. |
| G15 | `blueprint_not_found` named the problem and no action, so a weak model reissued the identical call until its step limit - twenty times, measured. | Same source; the general form was already known here from pin names | **Solved** | The error now names both possible causes - not created yet, or a wrong path - and what to do about each, including that repeating the call unchanged cannot work. |
| G16 | No way to ask what variables a Blueprint has. Components were listable; variables were not, so the only route was the project search index, which lags a write and reports false negatives. | Found while fixing the benchmark's own false failures | **Solved** | `list_variables` reads them directly, with types, defaults, category and per-instance editability. A direct read cannot lag. |
| G17 | A weak-model profile that offers both a complete tool and a worse-but-familiar one loses to the familiar one, and the model then cannot finish. | Measured here twice: once when `minimal` was built, and again when the same mistake was found still live in `lazy` | **Solved** | `unreal_create_blueprint` is out of the `lazy`/`core` surface as well as `minimal`. The feature task went 0/3 to 3/3 and from 16 calls to 9. Still reachable through `unreal_enable_tools`. |
| G12 | No way to drive data-driven gameplay: an MCP can make Blueprints but not the Data Tables they read, so every new item, weapon or dialogue line is new graph work rather than a new row. Competing tools advertise exactly this as the thing they do. | [Ludus AI feature comparisons](https://ludusengine.com/), Data Tables being the standard Unreal data-driven mechanism | **Solved** | `create_data_table`, `add_data_table_row` and `list_data_table_rows`, built on the existing struct support. Field names are validated before a row is written, the stored row is read back rather than echoed, and reads are paged because this is the one asset built to get large. |
| A8 | Client setup breaks in ways that all look identical: a missing comma in the config JSON, a relative path that does not resolve, or a bare `node` that is not on the client's PATH. The server simply never starts. | [Claude Desktop Windows setup guides](https://www.clauder-navi.com/en/claude-windows-mcp), [MCP config troubleshooting](https://www.ekamoira.com/blog/gsc-mcp-server-setup-complete-troubleshooting-guide-for-claude-desktop) | **Solved** | `node dist/index.js --print-config` emits the exact JSON for this machine, with absolute paths resolved and the command set to the absolute path of the Node that ran it, so it cannot be the wrong one. Supports Claude Desktop, Cursor and Claude Code, and names the file it goes in. Nobody should hand-write this. |
| A9 | A correct config appears not to work because the client was closed rather than fully quit. | Same sources; the most commonly cited cause | **Solved** | The printed config says so directly, above the JSON, where it will actually be read. |
| A7 | "Unable to start" / "Can't connect to MCP server in Cursor" with no further detail. | [chongdashu/unreal-mcp #32, #23](https://github.com/chongdashu/unreal-mcp/issues) | **Solved** | `unreal_doctor` checks reachability, protocol match, editor responsiveness, index state, node catalog, and PIE state in one call, each with its remedy, and never throws: an unreachable editor is the answer rather than an error. It also runs as `node dist/index.js --doctor` with no MCP client involved, which is the fastest way to find out which half of "my AI tool cannot see Unreal" is broken. |

## B. Engine version fragility

| # | Complaint | Source | Status | What this project does |
| --- | --- | --- | --- | --- |
| B1 | Plugin fails to compile on the user's engine version (≤5.4, 5.5+, 5.6, 5.7.4 all reported separately). | [chongdashu #48, #43, #31](https://github.com/chongdashu/unreal-mcp/issues), [ChiR24 #542, #492](https://github.com/ChiR24/Unreal_mcp/issues) | **Partly** | One source tree builds for both UE 5.6 and 5.8 with zero source changes, and every C++ change is compiled against **both** engines before it is committed, not just the newest one. The UMG work is a live example: the obvious 5.8 API for creating a Widget Blueprint does not exist on 5.6, so the older portable sequence was used instead. Prebuilt binaries are published per engine version so most users never compile anything. Not yet verified on 5.7. |
| B2 | The plugin silently refuses to load because `.uplugin` pins an `EngineVersion` the build tools ignore but the runtime loader honors. | Found in this project's own UE 5.6 bring-up | **Solved** | The hard pin was removed. This one is worth naming because every build check passed while the editor stopped on a modal incompatibility dialog and the bridge never started, which presents to the user as complaint A3. |
| B3 | A C++ signature change leaves Blueprints full of `in use pin X no longer exists, please refresh node`. | Widely reported UE-wide, not MCP-specific | **Solved** | `unreal_refresh_blueprint` performs the right-click Refresh Nodes repair across a Blueprint and reports the before/after error count. |

## C. Stability and destructiveness

| # | Complaint | Source | Status | What this project does |
| --- | --- | --- | --- | --- |
| C1 | The editor hard-crashes on asset/node creation. | [ChiR24 #499](https://github.com/ChiR24/Unreal_mcp/issues) | **Solved** | Node creation goes through `unreal_find_node`'s reflection-verified catalog, so an unknown name fails with `didYouMean` rather than reaching the engine with garbage. One crash of exactly this family was then found in **this** project by live verification and fixed: `create_blueprint` checked the disk for a name collision (`DoesPackageExist`) while the engine asserts on memory, so create-after-delete in one session closed the editor. All four create paths now check memory first and return `asset_name_in_use`. Covered by a regression check in `npm run verify:live`. There is now a dedicated crash sweep too, `npm run fuzz:crash`: every node type the bridge places plus 300 real functions from the live engine catalog, and adversarial input on every create path. It found a second crash of the same family - a 512-character asset name asserting on `FName`'s 1023 limit, because the object path repeats the name and counts it twice - now prevented by path validation. Current build: 364 accepted, 113 refused cleanly, **0 crashes**. |
| C2 | `execute_python` fatal crashes are undiagnosable. | [ChiR24 #525](https://github.com/ChiR24/Unreal_mcp/issues) | **Solved (by design)** | There is no arbitrary code execution path. Every command is a typed operation with structured errors. |
| C3 | The agent modified my Blueprints and I cannot get back to where I was. | Recurring concern across projects; flagged in this project's own competitive survey | **Solved (verified)** | Every write runs inside a named `FScopedTransaction` ("MCP: Add Node"). Claimed since M2 and never checked from outside the process until now: `unreal_undo_history` reads the editor's real transaction buffer, and live verification asserts that after a write the newest undo entry is ours. It reports 7 MCP entries with the next Ctrl+Z reversing "MCP: Add Variable". `unreal_undo_history` is also a user-facing answer to "what can I take back, and in what order?" |
| C4 | The agent deleted something still in use. | Recurring concern | **Solved** | `unreal_delete_asset` is blocked by default if anything outside the delete set still references the target, and returns the blocking referencers. `force: true` is required to override. |
| C5 | An asset path the model invented silently resolves to `None`, leaving a broken Blueprint that still compiles. | Found in this project's own live sessions | **Solved (verified)** | Property writes fail loudly on an unresolved asset path, and the error points at `unreal_list_assets`. Auditing this found the guard was only half right: it detected the bad path *after* `ImportText` had already written null, so a refused call still left the property as `None` - loudly reported, but done. The previous value is now restored, so a refusal really does change nothing, and live verification asserts the error says so. |
| C6 | A timeout leaves the caller unsure whether the write landed, so it retries and applies it twice. | Generic to every MCP over a stateful editor | **Solved** | Timeouts are per-command and sized to the operation, and the timeout message states explicitly that a timeout is not a rollback and that current state must be read before retrying a write. |
| C8 | A newly added command inherits a timeout too short for what it does, so working calls report failure. | Found in this repo by live verification | **Solved** | The timeout policy was inverted: cheap reads are enumerated and everything else, including every future command, gets a generous default. The old short-default-plus-exceptions list had already gone stale for the UMG commands, which recompile a Blueprint and were being cut off at 8s. |
| C7 | The tool answers confidently while the editor is still scanning, so a search reports that an asset does not exist when it does. | Found in this repo; generic to AssetRegistry-backed tooling | **Solved** | `unreal_doctor` surfaces `assetRegistryStillScanning` explicitly and says what it invalidates. A wrong answer delivered confidently is worse than a clean failure, so it is worth a dedicated check. |

| C12 | Deleting the level that is currently open hangs the editor: it blocks on a modal, the bridge stops answering, and the caller sees a timeout on a command that will never finish. | Found in this repo by live verification | **Solved** | `delete_asset` refuses with `cannot_delete_open_level` and tells you to open a different level first. |
| C13 | A deleted asset's name stays unusable for the rest of the session, because the object lingers until garbage collection. | Found in this repo by running live verification twice in one session | **Solved** | `delete_asset` now collects garbage after a successful delete, so delete-then-recreate simply works. The in-memory name guard that reported the problem was right to refuse - creating over a resident object asserts and closes the editor - so the fix is to actually free the name rather than to weaken the guard. |
| C14 | A creation command validates its inputs after creating the asset, leaving half-built debris behind when it fails. | Found in this repo by the same repeat run | **Solved** | `create_material` now resolves its textures before creating anything, the rule `create_struct` already followed. Live verification is repeatable in one session as a result, which is how the bug surfaced at all. |
| C10 | An engine API reports failure for work that succeeded, and the tool relays the lie. | Found in this repo by live verification | **Solved** | `UMaterialEditingLibrary`'s three material-instance setters declare `bool bResult = false;`, never assign it, and return it, on both 5.6 and 5.8: they always report failure, including when they succeed. Trusting that bool meant the parameter WAS written and the caller was told it had not been. Parameter existence is now checked directly against the material's own parameter list and the return value is ignored. The lesson generalises: an engine API's success flag is a claim, not evidence. |
| C11 | With two editors open, the MCP silently affects the wrong project: work meant for project A lands in project B with no error and no symptom. | [CoplayDev/unity-mcp #1023](https://github.com/CoplayDev/unity-mcp/issues/1023) | **Solved** | The bridge binds one port, so this project was fully exposed to it. Three defences: `ping` now reports the project name, `.uproject` path and engine version, and `unreal_doctor` names the connected project on every run; `UNREAL_MCP_EXPECT_PROJECT` checks the first **write** of the session and refuses with nothing sent on a mismatch; and the editor that loses the port now logs that another editor holds it and that this one's bridge is not running, instead of a bare bind failure. Verified end to end: a create call against a mismatched project name was refused before touching anything. |
| C18 | On a team project the agent's saves fail or silently lose work: source control marks un-checked-out files read-only, and a Blueprint is a **binary** `.uasset` that cannot be text-merged afterwards. | [Perforce on UE binary assets](https://www.perforce.com/blog/vcs/how-use-unreal-engine-5-perforce), [Diversion on UE merge conflicts](https://www.diversion.dev/blog/how-to-handle-merge-conflicts-in-unreal-engine-5) | **Solved (verified)** | Saving now checks the file first. If it is read-only and source control is connected, the file is checked out automatically; if it is read-only and source control is not available, the save is refused with what is actually wrong and the reassurance that **the edits are still live in the editor**, so the work is not redone. Verified against a genuinely read-only `.uasset`. |
| C20 | You only discover an asset is locked by a teammate after doing the work, because nothing checks first. | Consequence of Unreal's binary assets and checkout locking | **Solved** | `unreal_asset_status` answers writability up front and names who holds the checkout, so the answer becomes "BP_Door is checked out by alice, shall I work on something else?" rather than a pile of unsaveable edits. Deliberately a separate call, not a check inside every write: querying source control can hit the network, and paying that per node placement would slow the common case to protect the rare one. It is now step 3 of the golden path on shared projects. |
| C19 | Nobody finds out source control will block their saves until a save fails. | Consequence of C18 | **Solved** | `unreal_ping` reports whether source control is enabled and connected, and `unreal_doctor` warns when it is enabled but disconnected - before any work is done rather than after. |
| C9 | Giving an AI direct control of an engine's core systems introduces failure modes that do not exist when a human is clicking the buttons: the human always knows what they touched. | [Gameenginehub on AI-native workflow](https://gameenginehub.com/insights/unreal-engine-ai-epic-mcp-developer-assistant-ai-native-workflow), [StraySpark](https://www.strayspark.studio/blog/aura-vs-mcp-ai-assistants-unreal-engine-2026) | **Solved** | `unreal_session_changes` reports everything the server changed this session, grouped by asset, in plain language rather than command names, with deletions and failures surfaced separately. Undo already existed; undo is useless if you cannot see what there is to undo, and the user this is aimed at cannot read a Blueprint diff. Recorded by wrapping the transport, so the log cannot drift from what was actually sent, and the report states its own limits (it does not see hand edits or other tools) rather than leaving that to be discovered. |

| C15 | MCP servers accept any path a caller supplies, so a steered or mistaken agent can destroy things far outside the project. A 2025 survey found 43% of popular servers with command-injection flaws and 22% allowing path traversal. | [Checkmarx MCP security review](https://checkmarx.com/learn/mcp-security-risks-real-world-incidents-and-security-controls/) | **Solved (verified)** | This bridge was exposed: nothing confined writes to the project. Creating, modifying and deleting are now refused outside `/Game`, checked once at dispatch rather than in thirty handlers. Reads stay unrestricted because engine content is useful and harmless to read. Live verification asserts engine deletes and creates are refused while engine reads still work. |
| C16 | Prompt injection: a sentence planted in project data steers the agent, with no malware and no stolen credentials. | [Checkmarx](https://checkmarx.com/learn/mcp-security-risks-real-world-incidents-and-security-controls/), widely reported | **Partly** | Not preventable at the tool layer: a model reads Blueprint titles, node comments and asset names, and any of them can carry an instruction. The design bounds the damage instead - confined to `/Game`, undoable under an `MCP:` prefix, listed by `unreal_session_changes`, and unable to delete something still referenced without an explicit `force`. The README states this limitation rather than implying the problem is solved. |
| C17 | A security control the agent can switch off is not a control. | Design principle, from the same reviews | **Solved (by design)** | The only way to write outside `/Game` is relaunching the editor with `-MCPAllowEngineWrites`: a switch on the editor, not a tool parameter and not an environment variable this server reads. A human choosing it is a decision; anything the agent could flip on its own would be an exploit. |

## D. Token cost and context bloat

| # | Complaint | Source | Status | What this project does |
| --- | --- | --- | --- | --- |
| D1 | "Your MCP server is eating your context window": tool definitions alone consume the budget before the user's message is read. | [apideck](https://www.apideck.com/blog/mcp-server-eating-context-window-cli-alternative), [Albato on MCP context bloat](https://albato.com/blog/publications/embedded-mcp-context-bloat-hallucinations) | **Solved** | Measured first: the full set is 57 tools and ~18k tokens, of which 41% is tool descriptions and 17% parameter prose. Shortening them was rejected on measurement, since it buys about a tenth of the total and costs every model its sequencing guidance. `UNREAL_MCP_PROFILE=lazy` instead registers everything with full schemas but starts with only the 18-tool authoring path enabled (~5.7k tokens, **63% less**); the model calls `unreal_enable_tools` for a group and the server notifies the client that the list changed. Nothing is collapsed behind a dispatcher, so no schema fidelity is lost. `core` remains for clients that ignore `tools/list_changed`. |
| D7 | Tool-definition size is treated as a token cost, when on a local setup it is a hard gate on which models can run at all. | Measured here: a 14B loads on a 12 GB card at 8k context and fails at 16k, while the `lazy` profile is ~9.5k tokens of definitions by itself | **Solved** | `UNREAL_MCP_PROFILE=minimal` is 10 tools and ~3.6k tokens: the authoring spine only, with everything else reachable through `unreal_enable_tools`. This is the difference between a 14B being usable on a 12 GB GPU and not loading. `npm run check:profiles` measures every profile and fails the build if one grows past the ceiling its model can hold, because the original figures were hand-measured and went stale within a few commits. |
| D5 | Building one system burns hundreds of thousands of tokens; the same feature in C++ would cost twenty thousand. | Raised by this project's owner; the standard objection to agentic engine tooling | **Solved** | `UNREAL_MCP_MODE` sets how much a build spends, measured on a real build by `npm run measure:cost`: `fast` ~110 tokens per build response, `standard` ~172, `max` ~808. Combined with `UNREAL_MCP_PROFILE=lazy` the standing cost drops from ~17.7k to ~6.6k. Response payloads were the unexamined axis: tool definitions are paid once, responses are paid on every call. |
| D6 | Cheaper modes in AI tooling quietly produce worse work, and the user who chose cheap is least able to tell. | Generic to tiered AI products | **Solved (by design)** | The floor never moves. Every mode still builds atomically, lays the graph out, compiles, and fails loudly. Modes change polish and reporting only, and a test asserts every mode keeps auto-layout on. `fast` also states in its own description that the review is no longer attached and must be called explicitly, so the trade is visible rather than silent. |
| D2 | Reading an existing Blueprint dumps enormous raw engine data. | The problem this project was started for | **Solved** | Tiered reads: list Blueprints, then graphs, then a per-node summary with connected pins only, then full detail for exactly one node. No raw engine dump is ever returned. |
| D3 | Re-scanning the project on every question. | Competitive survey of 9 projects | **Solved (verified)** | `FMCPProjectIndex` builds once, caches to disk, and stays fresh from `AssetRegistry` delegates rather than polling or rescanning. Live verification creates an asset and confirms search finds it with no rescan and no restart, normally within a few milliseconds. One honest caveat the audit surfaced: the update is **asynchronous**, so a search issued in the same breath as a create can miss it, and right after heavy asset churn it can take a second or two to settle. Poll briefly rather than concluding the asset does not exist. |
| D4 | Mechanical indexing work burns frontier-model tokens. | This project's design goal | **Solved (optional)** | Point `UNREAL_MCP_LOCAL_LLM_URL` at Ollama or anything OpenAI-compatible and indexing summaries are generated locally, for free. Entirely optional. |

## E. The model does not know Unreal

| # | Complaint | Source | Status | What this project does |
| --- | --- | --- | --- | --- |
| E1 | The model invents node names, function names, and pin names that do not exist. | Universal; the most common cause of a failed edit | **Solved** | `unreal_find_node` and `unreal_get_node_signature` read the running engine's real Blueprint-callable surface via reflection (12,402 functions on 5.6, 15,775 on 5.8). Answers are correct for the engine version actually open, not recalled from training. |
| E2 | Wrong function name produces a cryptic engine error and a dead end. | Same | **Solved** | `unreal_add_node` returns `didYouMean` near-misses from the catalog. Worth recording how this row was wrong for months: the plugin produced the suggestions correctly, and the MCP server's transport discarded every field of a failure except the message, so no model ever saw one. Found by auditing this row rather than trusting it. The transport now preserves all failure context, which also restores available-pin lists and blocking-referencer lists that were being dropped the same way. |
| E7 | Self-hosted and smaller models (Qwen, DeepSeek, local Llama) can code but were never trained on Unreal, so they flail on engine specifics and are effectively unusable with these tools. | Raised by this project's owner; the standard objection to using local models for engine work | **Solved** | Two handbooks ship as MCP prompts, needing no configuration: `unreal_handbook` (mental model, class hierarchy, references, interfaces, type descriptors, multiplayer, performance, the specific traps) and `unreal_recipes` (complete verified builds of the systems people ask for). Every function name in the recipes is machine-checked against the running engine by `npm run verify:handbook`. |
| E15 | A small model cannot hold a plan across turns: it completes step one, declares the task done, and repeats its first successful call. | Measured with 7B and 14B locally; doubling parameters changed nothing | **Solved (verified)** | Not fixable inside a tool, so the tools stopped requiring it. `unreal_scaffold_blueprint` builds a whole Blueprint - variables, components, handlers - in one call and in the right order; the `minimal` profile then **removes `create_blueprint`**, because offering a worse-but-familiar path to a weak model is offering a way to fail. Measured on `qwen2.5-coder:7b`: **0/5 to 5/5** on a four-step task (10/10 across two sets) and **0/5 to 5/5** on graph wiring, which had never passed once. |
| E13 | Graph wiring is where a cheap model stops working: it knows what should happen but cannot get refs, pin names and nested JSON right in one call. | Measured with `qwen2.5-coder:7b` against a live editor | **Solved** | `unreal_add_event_handler` takes only the sequence - an event and the calls that follow - and wires the execution chain itself. No pin names, refs, or connections in the input, so there is nothing in the input to get wrong. Verified end to end with deliberately wrong pin names, which the bridge resolved. |
| E14 | A better tool nobody finds does not exist: the model keeps using the harder one it already knows. | Observed in the same benchmark | **Solved** | The redirect lives inside `unreal_build_graph`'s own description, where the model is already looking, rather than only in the new tool's. Building the better path is half the work; pointing at it from the point of confusion is the other half. |
| E11 | A near-miss pin name is a dead end for a weak model: the error names the right pin, and the model reissues the identical wrong call until it gives up. | Found by benchmarking `qwen2.5-coder:7b` against a live editor; it repeated one failing call **eleven times** | **Solved (verified)** | Pin resolution accepts a near-miss and reports the correction: case- and separator-insensitive matches, common execution-pin aliases, and - only when the node has exactly one execution pin of that direction - that pin. Corrections come back in `pinNamesCorrected` so the caller learns the real name rather than being silently carried. The eleven-call loop became one successful call and the task ran 5x faster. See [LOCAL_MODEL_BENCHMARK.md](LOCAL_MODEL_BENCHMARK.md). |
| E12 | "Works with any model" is asserted by every tool in this space and demonstrated by none. | This project's own headline claim | **Solved (verified)** | `npm run bench:local --runs 5` drives the server with a local 7B through a real agent loop and checks the outcome against the project, not the transcript. Result after shaping the tools for it: **5/5 on both benchmark tasks**, at ~20 tok/s on an RTX 3060 shared with the editor, with zero malformed arguments and zero invented tool names. Getting there required removing a worse path, not adding capability. Scope stated honestly: these are single features with clear descriptions, not system design. |
| E8 | Documentation aimed at models confidently names nodes that do not exist, and the models least able to notice are the ones relying on it. | Found in this repo: the first handbook draft had 7 wrong names out of 26 | **Solved** | `verify:handbook` asks the live catalog about every claim. It caught UE5's float-to-double math rename, `K2_GetActorLocation`, that Create Widget and runtime Spawn Actor are native nodes rather than functions, and that the `SpawnActorFromClass` in the catalog is editor-only and silently does nothing in a packaged game. |
| E3 | The calling agent has to rediscover the right tool-call order every session. | Adopted from the competitive survey | **Solved** | [AGENT_WORKFLOW.md](AGENT_WORKFLOW.md) ships the working call order, the exec-pin sharp edges, and the compile-before-claiming-done rule. |
| E4 | Can't create a Blueprint from a specific parent class (e.g. Character). | [chongdashu #21](https://github.com/chongdashu/unreal-mcp/issues) | **Solved** | `unreal_create_blueprint` takes any parent class by name or path, resolved through the engine's own class registry. |
| E9 | The tool is a code generator, not a colleague: it never says "you already have this", it just builds a second one. | Raised by this project's owner; the recurring ask for AI that behaves like an employee | **Solved** | `unreal_plan_feature` takes the request in the user's own words, checks every concept against the project, and returns `raiseWithUser` - what already exists, and what a change would reach outside its own system. It is step 1 of the golden path, and the workflow guide instructs the model to relay those points and wait for an answer rather than treating them as advisory. |
| E10 | New assets ignore the project's existing naming and folder conventions, so agent work is visibly foreign. | Generic to AI tooling on existing codebases | **Solved** | `unreal_plan_feature` reports the naming prefixes, folders and parent classes the project actually uses, derived from what is there rather than from a style guide nobody wrote. |
| E5 | On a real, months-old project, one Blueprint is wired to five others and there is no way to convey that to a model. Chat without tools cannot be told the context; tools that read one asset at a time make the model rebuild the shape by hand. | Raised by this project's owner about an 8-month project; the most common complaint about AI tools on existing codebases generally | **Solved** | `unreal_map_system` takes a concept and returns the assets that make up that system, the reference edges between them, which assets are risky to change, and what to read first. Built from the index and dependency graph with no graph reads, so mapping a twenty-asset system costs a fraction of reading one Blueprint. It is the first call in the golden path on an existing project. |
| E6 | The agent reads the first matching asset, assumes it is the whole system, and edits it, breaking things it never looked at. | Consequence of E5; reported across AI coding tools | **Solved** | The map's `highRisk` list names assets with referencers outside the system, and the workflow guide instructs the model to extend an existing system rather than add a second one, and to report what it found before changing anything widely used. |

## F. Output quality: functional but not AAA

| # | Complaint | Source | Status | What this project does |
| --- | --- | --- | --- | --- |
| F1 | "Blueprint spaghetti that spirals out of control" — AI output compiles but no human can read it. | [StraySpark, Aura vs MCP 2026](https://www.strayspark.studio/blog/aura-vs-mcp-ai-assistants-unreal-engine-2026) | **Solved** | `unreal_build_graph` auto-lays-out what it built, by default, with no coordinates from the caller: layered left-to-right ranking, crossing reduction, exec-chain straightening, and vertical separation between chains. The model cannot emit a pile at the origin even if it tries, because it does not choose the positions. |
| F2 | Generated graphs have no comments, default positions, no grouping, no naming discipline. | Same, and this project's own M7 milestone | **Solved** | Positions and grouping come from auto-layout; naming discipline is now checked: `unreal_review_blueprint` flags placeholder names (`NewVar`, `Temp`, `Test`), unlabelled sections, dead nodes, and empty events, each with the fix. Per-node explanatory comments remain the model's call, but a graph that lacks structure no longer passes silently. |
| F6 | The tool reports problems but never fixes them, so a weaker model reads the findings, says it has reviewed the Blueprint, and moves on. | The quality gate's own failure mode, one step later | **Solved** | `unreal_cleanup_blueprint` applies the fixes that cannot change behaviour (dead nodes, layout, labelled sections), re-reviews, and reports the score before and after. What it will not touch is listed with the reason for each: healing an exec chain around a removed Print String, choosing a variable name, deciding what an unhandled cast should do. The narrowness is deliberate - a cleanup that quietly changes behaviour is far worse than one leaving work on the table, because whoever runs it is least able to notice. |
| F5 | Nothing tells you where a large existing project needs attention; reviewing it means opening every Blueprint by hand. | Feature parity with [Ludus AI](https://ludusengine.com/), which advertises project-wide scanning for Tick overuse, costly casts and oversized Blueprints | **Solved** | `unreal_project_health` reports oversized graphs, oversized Blueprints and cast-heavy Blueprints across the whole project, computed from node-type histograms the index already keeps, so it costs **no asset reads**. Every finding says what it measured and every threshold explains itself. Deliberately absent: per-frame Tick, because the index stores node classes and cannot distinguish Event Tick from Event BeginPlay - guessing would produce confident false positives on the measure people most want to trust, and `unreal_review_blueprint` reports it properly from real titles. |
| F3 | A compiled Blueprint is treated as a finished feature, without anyone checking it runs. | Generic | **Solved** | `unreal_start_pie` / `unreal_pie_status` / `unreal_stop_pie` run the game, including multi-client sessions for replication. Compiling proves validity; running proves it works. |
| F4 | Nothing tells the model its output is bad, so it declares victory on work a reviewer would reject. | Generic to AI-authored code; the root cause behind F1/F2 | **Solved** | `unreal_review_blueprint` is the missing feedback signal: dead nodes, unhandled `Cast Failed` paths, leftover `Print String`, placeholder names, empty events, heavy Tick, oversized graphs. Findings carry the fix and the node ids; the report carries one `nextAction`. `unreal_build_graph` attaches it to its own result unasked, because the model most in need of the feedback is the one that would never ask. |

## G. Missing capability

| # | Complaint | Source | Status | What this project does |
| --- | --- | --- | --- | --- |
| G1 | Everything needed to turn a Blueprint into something that runs (levels, actors, components, class defaults, input, PIE) was implemented in the plugin but unreachable from any AI client. | Found in this repo, 2026-08-17 | **Solved** | 14 commands were registered as MCP tools, and `npm run check:parity` now fails the build if the two sides ever diverge again. |
| G2 | UMG / widget authoring support requested. | [chongdashu #26](https://github.com/chongdashu/unreal-mcp/issues) | **Solved** | `create_widget_blueprint`, `add_widget`, `list_widgets`, `set_widget_property` cover the whole loop: create the asset with a chosen root panel, build the tree, read it back, and set properties on either the widget or its layout slot. Build-verified on both UE 5.6 and 5.8 from one source. |
| G3 | Blueprint-to-C++ conversion requested. | [chongdashu #28](https://github.com/chongdashu/unreal-mcp/issues) | **Open** | Out of scope for now; noted. |
| G4 | Linux support unclear / undocumented. | [chongdashu #35](https://github.com/chongdashu/unreal-mcp/issues) | **Open** | The plugin has no platform-specific code and the server is Node, so it should work; nothing has been verified on Linux, so nothing is claimed. |
| G5 | Struct and enum authoring fails or is missing; `SetEnums` raises C2660 on UE 5.8. | [ChiR24 #566, #510](https://github.com/ChiR24/Unreal_mcp/issues) | **Solved** | `create_struct`, `add_struct_field`, `list_struct_fields`, `create_enum`, `list_enum_entries`, plus `struct:<Name>` / `enum:<Name>` variable types so a created struct is actually usable. The `SetEnums` trap is routed around rather than worked around: its signature genuinely differs between 5.6 and 5.8 (5.6 takes `EEnumFlags, bool`; 5.8 takes `UEnum::EUnderlyingType, EEnumFlags, EAddMaxKeyIfMissing`), so no single call compiles against both. `FEnumEditorUtils`/`FStructureEditorUtils` sit one level above and are identical on both, verified header to header. |
| G6 | Headless / editor-not-running mode. | Competitive survey | **Open** | The architecture requires a running editor. A real limitation, stated rather than hidden. |
| G7 | No Material authoring. | Gap against [tumourlove/monolith](https://github.com/tumourlove/monolith) and specialised material servers | **Partly** | `create_material` builds a master material with BaseColor/Metallic/Roughness/Emissive exposed as **parameters** rather than constants, so it is instanceable from the moment it exists; `create_material_instance`, `set_material_parameter` and `list_material_parameters` cover the instancing workflow real projects use. Live-verified on 5.6. Still open: arbitrary material graph authoring (texture sampling, blends, custom expressions), which is a much larger surface. |
| G8 | No Niagara / VFX authoring. | Same | **Partly** | Corrected after testing rather than assuming: *using* VFX works today. A `NiagaraComponent` can be added to any Blueprint and pointed at a system with `set_component_property`, and graphs can call `SpawnSystemAtLocation`, `SetAsset` and `Activate`. Recipe 8 covers it, with every name verified against the live engine. What is genuinely missing is **authoring a Niagara system from nothing**, which is a separate surface and rarely what someone with no coding experience needs. |
| G9 | No Animation authoring (montages, blend spaces, state machines). | Same | **Partly** | Same correction: *using* animation works. A `SkeletalMeshComponent` can be added and given a mesh and an Anim Blueprint (`SetSkeletalMeshAsset`, `SetAnimInstanceClass`), and `PlayAnimMontage` drives it from a graph. Recipe 8 covers it. Authoring the animation assets themselves is still out of scope. Testing this also surfaced a rename trap now documented: the obvious `SetSkeletalMesh` exists on three unrelated editor classes and none is the component you have. |

## Auditing the claims in this document

Rows here are only worth as much as their evidence. Several were written from reasoning about the
code rather than from running it, so `npm run verify:live` now includes an audit section that
exercises the load-bearing ones against a real editor.

The first audit found that **E2 had been false since it was written**. The plugin generated its
`didYouMean` suggestions perfectly; the MCP server's transport read only the `error` field off a
failed response and threw the rest away, so the single most useful self-correction signal in the
system had never once reached a model. Three separate documents claimed it worked.

Two lessons kept, both cheap:

- A claim nobody has exercised is a claim, not a guarantee. The audit section exists so these rows
  cannot quietly drift from the code again.
- The bug was not in the feature. It was in the seam between two components that were each correct
  on their own. Those are the ones reasoning does not catch.

## Checking before building

Three rows in this document (G8, G9, and part of G7) were written as **Open** on the assumption
that a capability was missing, and were corrected only after somebody actually tried it. Attaching
a `NiagaraComponent`, an `AudioComponent`, or a `SkeletalMeshComponent` and pointing it at an asset
worked on the first attempt, through tools that already existed.

That is worth recording as a habit rather than an anecdote. The cost of assuming a gap is not just
a wrong row in a table: it is building a redundant tool, which adds context cost for every user
forever and makes the real gap harder to see. **Test the capability before writing the tool.** When
the answer turns out to be "this already works", the deliverable is a recipe and a corrected row,
not code.

## A note on coverage

The ecosystem moved while this document was being written. [tumourlove/monolith](https://github.com/tumourlove/monolith)
now advertises 1,400+ actions across 25+ namespaces covering Materials, Niagara, Animation, GAS,
Audio and more, targeting UE 5.7 and 5.8, also native C++ with no Python dependency.

That is a far broader surface than this project has, and rows G7 through G9 above are the honest
consequence. What this project is betting on instead is a different axis: that the bottleneck for
someone with no coding experience is not how many asset types the tool can touch, but whether the
output is correct, readable, and safe. Hence tiered reads, verified node names, enforced layout, a
review gate, a crash sweep, and a change log. Breadth and those are not in conflict, and the gaps
are listed as gaps rather than argued away.

## How to add to this document

Add a row when you find a complaint in the wild, with its link, even if the answer is "Open". A
complaint with no row is a complaint nobody is tracking. Update the status when the code changes,
and keep **Open** rows honestly open: the value of this file is that it can be trusted.
